<p id="notice" class="text-center"></p> <!-- text-center を追加 -->
<div class="container">
  <div class="row justify-content-center"> <!-- justify-content-center を追加 -->
    <div class="col-sm-12 col-md-8 col-lg-8 px-5 py-5 px-sm-0 text-center">
      <span class="font-weight-bold px-2 h4">本当に退会しますか？</span>
      <div class="mt-3"></div>
      <div class="form-group row">
        <span class="col-12">退会すると、会員登録情報や<br>これまでの投稿が閲覧できなくなります。<br>退会する場合は、「退会する」をクリックしてください。</span>
      </div>
      <%= form_with model: @user, url: users_quit_path,local: true do |f| %>
      <div class="form-group row ">
        <div class="col-6 d-flex justify-content-end">
          <a href="<%= my_page_path %>" class="btn btn-primary">退会しない</a>
        </div>
        <div class="col-6 d-flex justify-content-start">
          <%= link_to "退会する", users_quit_path, method: :patch, :class => "btn btn-danger", data: { confirm: "本当に退会しますか？" }  %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
